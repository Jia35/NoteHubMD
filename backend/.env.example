# NoteHubMD Environment Configuration
# Copy this file to .env and modify as needed

# Application
APP_VERSION="0.1.0 beta"
APP_BASE_PATH="/"

# Server
PORT=3000

# Session (IMPORTANT: Change this in production!)
SESSION_SECRET="your-secret-key-here"

# Database
# For PostgreSQL (default):
DB_DIALECT=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password
DB_NAME=notehubmd
DB_LOGGING=false

# For SQLite (alternative):
# DB_DIALECT=sqlite
# DB_STORAGE=./database/database.sqlite
# DB_LOGGING=false

# Default Permissions (private, auth-view, auth-edit, public-view, public-edit)
DEFAULT_NOTE_PERMISSION=private
DEFAULT_BOOK_PERMISSION=private

# Features
# 筆記留言功能
FEATURE_COMMENTS=true
# 筆記表情回饋功能
FEATURE_NOTE_REACTIONS=true

# API Master Key (leave empty to disable)
API_MASTER_KEY=

# 留言 Webhook
# COMMENT_WEBHOOK_URL=https://your-webhook-endpoint.com/hook

# =====================
# LDAP / Active Directory
# =====================
# 啟用 LDAP 登入（設為 true 則只能用 AD 帳號登入，不開放註冊）
LDAP_ENABLED=false

# LDAP 伺服器 URL
LDAP_URL=ldap://your-ldap-server:389

# 管理員 DN（用於搜尋使用者）
LDAP_BIND_DN=cn=admin,dc=example,dc=com
LDAP_BIND_PASSWORD=admin-password

# 使用者搜尋設定
LDAP_SEARCH_BASE=ou=users,dc=example,dc=com
LDAP_SEARCH_FILTER=(sAMAccountName={{username}})

# =====================
# Proxy 設定 (選用)
# =====================
# 若需透過代理連線外部服務（如 AI API），請設定以下變數：
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

